# 의존성 / 빌드 산출물
node_modules
.next
out
*.tsbuildinfo
next-env.d.ts

# 배포 도구
.vercel

# 환경변수: 실제 값은 절대 커밋하지 않는다 (.env.example 만 예외)
.env
.env.*
!.env.example
.env.local.backup

# 구글 서비스 계정 키 (절대 커밋 금지)
seminar-reservation-*.json
*service-account*.json
*-key.json
